# Add app-specific ProGuard/R8 rules here.
# Start with an empty file and add rules if build warnings indicate necessary keeps.
# Example (uncomment if needed):
# -keep class armmel.home.** { *; }
# -dontwarn okio.**
